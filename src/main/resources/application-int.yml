# Configuration applied only when 'int' profile is active.
#
# Here are the environment dependant configurations.

# Server.
server:
  port: 8001 # Use 0 for a free random port.

# Logging configuration.
logging:
  level:
    es.apinazo: DEBUG
    root: INFO

spring:

  jpa:
    hibernate:
      ddl-auto: none

  datasource:
    url: jdbc:mysql://int/bootbase
